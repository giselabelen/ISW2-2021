//EJERCICIO 8

P = (a -> b -> P).
Q = (c -> b -> Q).
||S = (P || Q).

// Descomentar el inciso que se quiere probar
// (es para evitar problemas por las composiciones)


//8a

//U = (c-> V|a->W),
//V = (a->R),
//W= (c->R),
//R = (b->U).


//8b

//R =( a->c->R).
//||E = (S||R).


//8c

//T= (c->d->T|c->T).
//||E = (S||T).
